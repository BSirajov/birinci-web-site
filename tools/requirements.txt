# Site tooling (QA, chrome restore helpers, publish policy tests).
# API deps live in api/requirements.txt.
pytest>=8.0,<9
# Starlette TestClient (api/app/test_auth.py) — required on current starlette releases.
httpx2>=2.0.0
# Optional: full browser matrix — pip install playwright && playwright install chromium
playwright>=1.40,<2
